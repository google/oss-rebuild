FROM golang:1.26-alpine AS binary
ARG DEBUG=false
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -ldflags="$([ ${DEBUG} = "true" ] || printf '-s -w')" -gcflags="-l=4" ./cmd/stabilize
FROM scratch
COPY --from=binary /src/stabilize /
ENTRYPOINT ["/stabilize"]
