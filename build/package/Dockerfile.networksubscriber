FROM golang:1.24-alpine as binary
ARG DEBUG=false
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -ldflags="$([ ${DEBUG} = "true" ] || printf '-s -w')" -gcflags="-l=4" ./analyzer/network/subscriber
FROM alpine
COPY --from=binary /src/subscriber /
ENTRYPOINT ["/subscriber"]
