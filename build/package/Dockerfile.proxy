FROM golang:1.22-alpine as binary
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /proxy -ldflags="-s -w" -gcflags="-l=4" ./cmd/proxy
FROM scratch
COPY --from=binary /proxy  /proxy
ENTRYPOINT ["/proxy"]
