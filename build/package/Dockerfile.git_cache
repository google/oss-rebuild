FROM golang:1.22-alpine as binary
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /git_cache -ldflags="-s -w" -gcflags="-l=4" ./cmd/git_cache
FROM alpine
COPY --from=binary /git_cache ./git_cache
ENTRYPOINT ["./git_cache"]
