# inference fails: no git ref
pypi_pure_wheel_build:
  location:
    repo: https://github.com/eliben/pycparser
    # Tag is `release_v3.00` (prefix + zero-padded patch); `tools/ctl infer`
    # rejects it because the boundary regex won't match `3.00` against `3.0`.
    ref: 77de509f0268f44ee587b5a4d9f0d680e269fcae
  requirements:
    - setuptools
    - wheel
  registry_time: "2026-01-21T14:26:50Z"
